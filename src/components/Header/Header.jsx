import React from "react";
import "./Header.scss";

export default function Header() {
  return (
    <header>
      1. room filter 2. font ban 3. save 4. сортировку по статусу 5. server --
      list [input] || server--list add
    </header>
  );
}
